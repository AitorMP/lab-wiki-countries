import React from 'react';

const NavBar = () => {
  return (
    <div>
      <nav class="navbar navbar-dark bg-primary mb-3">
        <div class="container">
          <h1>WikiCountries</h1>
        </div>
      </nav>
    </div>
  );
};

export default NavBar;
